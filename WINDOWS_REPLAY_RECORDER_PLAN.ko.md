# Windows 최근 화면·오디오 저장 프로그램 상세 개발 계획서

- 작성일: 2026-09-09
- 프로젝트 가칭: ReplayCapture
- 문서 상태: 구현 전 설계 제안. 성능 수치와 일정은 목표·추정치이며, 검증 완료 결과가 아니다.
- 산출 범위: 요구사항, 기술 선택, 내부 구조, GUI·단축키·설정 계약, 구현 단계, 테스트, 배포 기준.

## 1. 목표와 기본 동작

Windows에서 사용자 로그인 세션의 백그라운드 프로그램이 모니터 화면과 소리를 계속 캡처한다. 최근 일정 시간의 압축 데이터를 순환 버퍼에 보관하고, 사용자가 GUI의 저장 버튼을 누르거나 지정한 단축키를 입력하면 해당 요청 접수 시점 직전의 기록을 영상 파일로 저장한다.

기본 저장 시간은 60초이며 사용자가 변경할 수 있다. 예를 들어 14:30:00에 GUI의 [최근 60초 저장] 버튼 또는 저장 단축키를 누르면 원칙적으로 14:29:00~14:30:00 기록을 저장한다. 저장 이후에도 캡처와 순환 버퍼 갱신은 계속된다.

프로그램이 실행되기 전, 일시정지 중, 절전 중의 화면을 복원하는 기능은 제공할 수 없다. 충분한 기록이 없을 때는 실제 보관된 길이를 명시해 저장한다.

## 2. 요구사항과 초기 제품 범위

| ID | 요구사항 | 초기 구현 기준 |
|---|---|---|
| R01 | Windows 지원 | Windows 11 x64를 우선 검증 대상으로 설정 |
| R02 | 백그라운드 녹화 | 사용자 세션의 트레이 프로그램, 일반 사용자 권한 |
| R03 | 최근 기록 저장 | 기본 60초, GUI 버튼과 전역 단축키 모두 필수 |
| R04 | 소리 포함 | 선택한 출력 장치의 시스템 소리 기본 포함 |
| R05 | 시간 설정 | 보관 시간과 기본 저장 시간을 각각 변경 |
| R06 | 저장 중 녹화 | 파일 생성과 캡처 파이프라인 분리 |
| R07 | 일반 영상 파일 | MP4, H.264 영상 + AAC 오디오 |
| R08 | 화면 선택 | 연결된 모니터 중 하나 선택 |
| R09 | 상태 확인 | 실제 보관 시간, 녹화·오디오 상태, 저장 작업 상태 표시 |
| R10 | 자원 제한 | 압축 버퍼·작업 큐·총 메모리 상한 적용 |
| R11 | 모든 기능의 GUI 조작 | 시작·중지·저장·설정·작업 관리·오류 확인을 GUI로 제공 |
| R12 | 단축키 사용자 변경 | GUI에서 키 지정·충돌 확인·적용·테스트·복원, 재시작 후 유지 |

### 2.1 사용자에게 제안하는 기본값

- 모니터: 최초 실행 시 선택, 이후 식별 정보로 재연결.
- 시스템 소리: 기본 출력 장치를 따라감. 다른 출력 장치로 재생되는 소리까지 모두 합산하지는 않는다.
- 마이크: 기본 꺼짐. 초기 핵심 기능 안정화 후 선택 기능으로 추가.
- 보관 시간: 60초. UI 설정 범위 제안은 5~600초이며 메모리 예산 내에서만 수락.
- 기본 저장 시간: 보관 시간과 같은 60초. GUI에서 이번 저장 길이를 더 짧게 선택 가능. 단축키는 기본 저장 시간을 사용.
- 영상: 최대 1920×1080, 원본 종횡비 유지 및 필요 시 레터박스, 30fps, SDR.
- 영상 비트레이트: 8Mbps 목표. 출력 오디오: 48kHz 스테레오, AAC 192kbps.
- 단축키: `Ctrl+Shift+F9` 제안. 사용자가 변경할 수 있고 충돌을 표시.
- 자동 실행: 기본 꺼짐, 사용자가 설정에서 활성화.

이 값들은 사용자 요구를 구현 가능한 형태로 구체화한 제안이다. 4K·60fps, 다중 모니터 동시 합성, HDR 보존, 개별 앱 오디오, 클라우드 업로드, 편집기는 후속 범위로 둔다. Windows 10과 ARM64 지원은 별도 요구가 생기면 지원 OS·장비·빌드 행렬을 추가한다.

### 2.2 완료 판정 예시

1. 테스트 화면과 시스템 소리를 90초 이상 캡처한다.
2. GUI의 [최근 60초 저장] 버튼을 누르고 작업 화면에서 완료를 확인한다.
3. 생성된 MP4에 저장 요청 직전 화면과 소리가 있고 정상 재생된다.
4. GUI에서 보관 시간을 120초로 늘리고 충분히 기다린 뒤 이번 저장 길이를 90초로 선택해 저장한다.
5. 단축키를 GUI에서 변경하고 다른 앱에 포커스를 둔 상태에서 기본 60초 저장을 확인한다.
6. 재시작 후 변경한 키가 유지되고 모든 기능을 GUI만으로 조작할 수 있는지 확인한다.
7. 저장 중에도 다음 저장에 필요한 화면이 연속해서 쌓인다.

## 3. 기술 선택

### 3.1 권장 구성

| 영역 | 선택 | 선정 이유와 확인 사항 |
|---|---|---|
| 언어 | C++20, C++/WinRT | Windows 캡처·D3D11·오디오·코덱 API를 같은 네이티브 엔진에서 연결 |
| 빌드 | CMake + MSVC + Windows SDK | GUI·코어·테스트 타깃 분리, 정확한 버전은 착수 시 고정 |
| 화면 | Windows.Graphics.Capture | 모니터 캡처, GPU 표면 처리 경로 사용 |
| 영상 처리 | Direct3D 11 | 크기 변환과 BGRA→NV12 변환, 불필요한 CPU 복사 최소화 |
| 시스템 소리 | WASAPI shared-mode loopback | 선택한 재생 장치의 시스템 믹스 수집 |
| 압축 | Media Foundation MFT | H.264 하드웨어 인코더 우선, AAC 오디오 인코더 |
| 버퍼 | 압축 샘플을 보관하는 메모리 링 | 지속적인 임시 디스크 기록 없이 최근 구간 유지 |
| 파일 생성 | Media Foundation MPEG-4 sink | 저장 시 보관한 압축 샘플을 MP4로 다중화 |
| 사용자 UI | Win32 메인 대시보드 + 설정·작업 화면 + 트레이 | 모든 기능을 GUI로 제공하고 비동기 엔진 상태 반영 |
| 저장 단축키 | 사용자 지정 전역 키 조합 | GUI에서 변경·충돌 확인, 창을 숨긴 상태에서도 저장 |

Windows.Graphics.Capture는 화면과 창의 프레임을 제공한다. 시작 시 지원 여부를 확인하고, 첫 실행은 시스템 선택 UI를 사용한다. 캡처 대상 크기 변경·장치 손실도 처리해야 한다. [Microsoft 화면 캡처 문서](https://learn.microsoft.com/en-us/windows/uwp/audio-video-camera/screen-capture)

WASAPI loopback은 재생 장치의 소리를 shared mode로 수집하는 기능이다. 별도의 하드웨어 루프백 장치가 없어도 사용할 수 있다. [Microsoft Loopback Recording](https://learn.microsoft.com/en-us/windows/win32/coreaudio/loopback-recording)

Media Foundation은 H.264 인코더와 MP4 sink를 제공하지만, 설치된 GPU·드라이버별 하드웨어 지원과 압축 샘플 직접 연결은 초기 실험으로 검증한다. [H.264 인코더](https://learn.microsoft.com/en-us/windows/win32/medfound/h-264-video-encoder), [MPEG-4 File Sink](https://learn.microsoft.com/en-us/windows/win32/medfound/mpeg-4-file-sink)

### 3.2 대안과 선택 변경 조건

| 대안 | 적합한 경우 | 이번 계획에서의 위치 |
|---|---|---|
| OBS 기반 제어 프로그램 | 기존 녹화 프로그램 의존을 허용하고 GUI·단축키 제어만 개발할 때 | 독립 프로그램 요구가 완화될 때 별도 검토 |
| FFmpeg 라이브러리 기반 엔진 | 팀에 미디어 경험이 있고 인코더·컨테이너 확장이 중요할 때 | MF MP4 연결 실험 실패 시 libavformat 등의 대체 검토 |
| C# UI + C++ 엔진 | 설정 UI가 복잡해질 때 | 이후 UI 교체 가능하도록 엔진 API 분리 |
| 짧은 디스크 세그먼트 순환 저장 | 수십 분 이상의 긴 기록이 필요할 때 | 2차 버퍼 백엔드 후보, 초기 구현에서 제외 |
| DXGI Desktop Duplication | 특정 환경에서 WGC 호환 문제가 확인될 때 | 별도 호환성 실험 후 추가, 초기 이중 구현 제외 |

대체 라이브러리를 채택하면 정확한 버전, 빌드 옵션, 재배포 조건과 패키지 크기를 다시 검토한다. 이 문서는 해당 대안의 현재 기능이나 호환성을 검증한 결과가 아니다.

## 4. 전체 구조

```text
모니터 → WGC → D3D11 크기·색 변환 → H.264 인코더 ─┐
                                                ├→ 압축 순환 버퍼
출력 장치 → WASAPI → 시간 정렬·리샘플 → AAC 인코더 ─┘       │
                                                        │ 구간 참조 고정
GUI 버튼 / 전역 단축키 / 트레이 → 동작 처리기 → 저장 작업 큐 ───┘
                                             ↓
                                타임스탬프 재배치 → MP4 sink
                                             ↓
                                  임시 파일 → 완성 MP4
```

### 4.1 프로세스와 스레드

- `ReplayCapture.exe`: 단일 사용자 세션에서 상주. 메인 GUI, 트레이, 설정·작업 화면, 단축키 관리자와 캡처 엔진을 소유한다.
- GUI와 단축키는 같은 프로세스의 공통 동작 처리기와 비동기 작업 큐를 사용한다. 사용자용 CLI 실행 파일과 외부 제어 서버는 만들지 않는다.
- 사용자·세션별 mutex로 중복 실행을 방지한다. 재실행 시 기존 GUI를 표시하는 용도로만 제한된 창 메시지를 사용하고, 녹화·저장 설정은 외부 메시지로 제어하지 않는다.
- UI/메시지 루프, 화면 프레임 처리, 오디오 캡처, 인코더 처리, 저장 worker의 실행 흐름을 분리한다.
- 모든 큐는 길이와 메모리 상한을 갖는다. 콜백에서 디스크 쓰기나 장시간 잠금 대기를 하지 않는다.
- 저장 worker는 초기 1개, 대기 작업은 최대 3개를 제안한다. 메모리 제한이 더 먼저 적용될 수 있다.

백그라운드는 창을 최소화해 사용자 로그인 세션에서 동작한다는 의미다. 화면 캡처 엔진을 Session 0의 Windows 서비스로 구현하지 않는다.

### 4.2 상태 모델

```text
Stopped → Starting → Buffering
                      ↕
                    Paused
Buffering → Recovering → Buffering 또는 Error
모든 상태 → Stopping → Stopped
```

저장은 별도 작업 상태 `Queued → Writing → Completed / Failed / Cancelled`로 관리한다. 저장 작업 때문에 캡처 상태가 `Stopped`로 바뀌지 않는다. 오디오 상태는 `Ready / Silent / Reconnecting / Unavailable`로 따로 표시한다.

## 5. 순환 버퍼와 시간 계약

### 5.1 데이터 모델

압축 샘플은 최소한 다음 정보를 가진다.

```text
EncodedSample {
  stream_id, epoch_id,
  pts_100ns, dts_100ns, duration_100ns,
  is_keyframe, codec_config_id,
  shared_payload
}
```

- PTS는 표시 시각, DTS는 디코딩 시각이다. 첫 구현은 B-frame 비활성 설정을 우선한다.
- 실제 출력에 재정렬이 존재하면 DTS/PTS 처리를 구현하거나 해당 인코더를 지원 대상에서 제외한다.
- 영상은 독립 디코딩 가능한 IDR을 기준으로 GOP 단위 관리한다. 단순한 I-frame 표기만 믿지 않고 출력 비트스트림·샘플 속성을 확인한다.
- SPS/PPS 등 코덱 구성과 AAC 설정을 샘플과 함께 유지한다.
- `epoch_id`는 모니터·해상도·인코더 등 연속성을 깨는 변경 때 증가한다. 서로 다른 epoch를 한 MP4에 무조건 이어 붙이지 않는다.

### 5.2 최근 N초 저장의 정확한 의미

변수는 다음과 같이 정의한다.

- `T`: 엔진이 저장 요청을 받은 단조 시계 시각.
- `D`: 요청한 저장 길이.
- `S = T - D`: 목표 시작점.
- `K`: S 이하의 가장 가까운 독립 디코딩 가능한 키프레임.

**초기 기본 모드는 빠른 저장이다.** 영상 재인코딩 없이 K부터 T까지 저장한다. 목표 GOP 간격은 1초이며, 실제 검증된 키프레임 간격이 1초 이하면 시작점은 목표보다 최대 약 1초 앞설 수 있다. 파일 길이는 프레임·오디오 패킷 경계만큼 추가 차이가 날 수 있다. 저장 요청 이후에 생성된 미래 화면을 길이를 맞추려고 덧붙이지 않는다.

따라서 초기 버전의 “최근 60초”는 60초 이전 키프레임부터 시작하는 약 60~61초 기록일 수 있다. GUI 저장 결과에 요청 길이, 실제 길이, 시작점 차이, 끝 시점 누락을 표시한다. 내부 결과 모델은 `requestedDuration`, `actualDuration`, `startOffset`, `endLag`를 사용한다. 길이 정확도가 필수인 경우 후속 **정밀 저장 모드**에서 K부터 디코딩하고 S 이전 화면을 버린 뒤 요청 구간 전체를 재인코딩한다. 이 모드는 느리고 저장 중 GPU 부하가 증가하므로 별도 성능 기준으로 출시한다.

오디오는 영상 시작·끝에 맞춰 AAC 패킷 단위로 선택한다. 경계 패킷·인코더 지연·패딩의 처리 결과를 재생기로 검증하고 허용 오차를 기록한다. 오디오와 영상의 시작 시각을 각각 독립적으로 0으로 만들지 않는다.

### 5.3 보관 및 삭제 정책

1. 논리적으로 최근 `retentionSeconds`를 보관한다.
2. 그 구간을 디코딩할 수 있도록 직전 IDR까지 추가 보관한다.
3. 인코더 출력 지연을 위한 작은 여유와 대응하는 오디오도 보관한다.
4. 시간 기준 삭제는 GOP 단위로 수행한다.
5. 동시에 하드 바이트 상한을 적용한다. 비트레이트 급증 시 실제 보관 길이가 짧아질 수 있음을 상태에 표시한다.
6. 메모리 부족으로 목표 길이를 충족하지 못하면 조용히 정상 처리하지 않고 `buffer_limited` 경고를 낸다.

설정값이 60초라도 반드시 매번 60초를 보장하는 것은 아니다. `availableDuration`은 현재 epoch의 실제 연속 저장 가능 길이로 계산한다.

### 5.4 저장 스냅샷과 경합 처리

- 요청 접수 시 T, 설정, epoch, 요청 ID를 고정하고 필요한 구간을 즉시 pin한다.
- 이미 버퍼에 들어온 샘플뿐 아니라 T 이전의 아직 인코딩 중인 샘플도 작업의 대상이다.
- 영상·오디오 처리 watermark가 T에 도달할 때까지 제한 시간 내 기다린다. 이후 시각의 프레임은 저장 대상에 포함하지 않는다.
- 초기 대기 상한은 2초로 제안한다. 초과 시 실제 끝 시각과 누락 경고를 반환하거나 전체 길이 필수 정책의 요청을 실패시킨다.
- `shared_payload` 참조를 공유해 전체 버퍼 복사를 피한다. 저장 중인 GOP는 삭제하지 않되 pin한 메모리도 총량에 포함한다.
- 예산을 넘길 새 요청은 `BUSY`로 거절한다. 받아 놓고 무제한 메모리를 소비하지 않는다.
- 작업 완료·실패·취소 때 참조를 해제한다. 캡처 락을 잡은 채 MP4를 쓰지 않는다.

## 6. 화면 캡처 상세

1. 지원 API·D3D11 장치를 확인하고 모니터 선택을 받는다.
2. WGC 프레임 풀과 세션을 생성한다.
3. 유효한 `ContentSize` 영역을 복사하고 목표 출력 크기·색 형식으로 변환한다.
4. 고정 30fps 타임라인에 맞춰 프레임을 샘플링한다. 정상 캡처 중 정지 화면에서는 마지막 유효 프레임을 반복해 시간 길이를 유지한다.
5. 프레임 원본과 풀 자원은 신속히 반환하고 GPU 작업 수를 제한한다.
6. 하드웨어 인코더를 열거·시험하고 실제 선택 결과를 진단에 기록한다.

정지 화면과 캡처 세션 장애는 구분해야 한다. 대상 종료·잠금·장치 손실 상태에서 오래된 화면을 계속 정상 녹화한 것처럼 반복하지 않는다. 해상도·회전 변경 시 풀을 재생성하고 epoch를 전환한다. HDR 입력은 초기 지원 범위 밖으로 명시하고, 감지 시 설정 안내를 제공한다. 색이 틀린 결과를 정상 지원으로 표시하지 않는다.

## 7. 오디오와 동기화

### 7.1 시스템 소리

- 기본 출력 또는 사용자가 지정한 장치의 loopback을 수집한다.
- 장치 mix format을 확인하고 내부 48kHz float PCM으로 변환한다.
- 데이터의 SILENT·DISCONTINUITY·TIMESTAMP_ERROR 플래그를 처리한다.
- 정상 무음 구간에는 0 샘플을 채워 오디오 시간축을 유지한다.
- 장치가 없거나 접근에 실패한 상태는 정상 무음과 구분한다. 초기 녹화 시작은 소리 필수 정책으로 실패시키고, 사용자가 명시적으로 영상만 허용할 때에만 진행한다.
- 실행 중 장치 분리 시 재연결을 시도하고 해당 구간은 무음 및 경고 메타데이터로 표시한다. 기본 장치 추적과 특정 장치 고정 모드는 구분한다.

### 7.2 공통 시계

WGC 프레임의 `SystemRelativeTime`은 QPC 기반이며, WASAPI `GetBuffer`는 QPC 위치를 100ns 단위로 제공한다. 두 입력을 동일 기준으로 정규화하고 원시 QPC tick과 혼용하지 않는다. [WGC 시각 속성](https://learn.microsoft.com/en-us/uwp/api/windows.graphics.capture.direct3d11captureframe.systemrelativetime), [WASAPI GetBuffer](https://learn.microsoft.com/en-us/windows/win32/api/audioclient/nf-audioclient-iaudiocaptureclient-getbuffer)

벽시계는 파일명에만 사용한다. 오디오 sample count와 QPC 사이 오차를 관찰해 완만한 리샘플링 보정으로 장기 drift를 제어하고, 큰 불연속은 epoch 변경 또는 명시적 gap으로 처리한다. 영상·오디오에 같은 타임라인 원점을 적용하며 인코더 지연은 POC에서 측정한다.

### 7.3 선택 마이크 기능

마이크는 별도 WASAPI 입력으로 받아 동일 시간축·형식으로 맞춘 뒤 시스템 소리와 혼합한다. 개별 음량, 음소거, clipping 방지 limiter를 제공한다. 자신의 녹음 신호를 출력 장치로 다시 재생하지 않는다. 스피커 소리가 마이크에 다시 들어오는 음향 에코 제거는 별도 기능이며 초기 지원을 약속하지 않는다.

## 8. 파일 생성과 실패 복구

1. 저장 폴더 접근과 여유 공간을 확인한다.
2. `Replay_20260909_143000_123_<request-id>.mp4.partial`을 최종 폴더에 만든다. 확장자 추론에 의존하지 않고 MP4 sink를 명시적으로 선택한다.
3. pin한 코덱 구성과 영상·오디오 샘플을 공통 원점 기준으로 재배치한다.
4. sink가 요구하는 순서·시간 정보로 샘플을 기록한다. 압축 데이터의 Annex B/길이 접두 형식 변환 및 코덱 헤더 전달도 검증한다.
5. finalize 후 파일을 닫고 트랙·길이를 점검한다.
6. 같은 디렉터리 내에서 최종 `.mp4` 이름으로 변경한 뒤에만 성공을 알린다.

기존 파일은 덮어쓰지 않는다. 파일명 충돌은 새 ID로 해결한다. 공간 부족·권한 오류·finalize 실패는 작업 실패로 반환한다. 실패한 partial 파일은 정리하고 실패 사유를 남긴다. 네트워크 폴더는 초기 성능 보장 범위 밖이며 우선 로컬 폴더를 사용한다.

강제 종료 시 메모리 버퍼는 사라진다. 시작 시 앱이 소유한 오래된 partial 파일을 제한된 범위에서 정리한다. 일반 MP4의 미완료 파일 복구는 보장하지 않는다. 이미 완료된 MP4는 자동 삭제하지 않는다.

## 9. GUI 및 사용자 지정 단축키

모든 사용자 기능은 GUI 화면에서 조작한다. 최근 기록 저장은 메인 화면 버튼과 사용자 지정 전역 단축키 양쪽에서 제공한다. 텍스트 CLI와 설정 파일 직접 편집은 사용자 조작 경로에 포함하지 않는다. 아래는 구현할 화면·동작 계약이다.

### 9.1 화면 구성과 기능 배치

| 화면 | 조작 가능한 기능 | 표시할 상태 |
|---|---|---|
| 메인 대시보드 | 녹화 시작, 일시정지, 재개, 녹화 중지, 최근 기록 저장, 이번 저장 길이 선택, 버퍼 비우기, 설정 열기, 앱 종료 | 녹화 상태, 목표·실제 보관 길이, 모니터, 오디오 상태, 현재 저장 단축키 |
| 설정 → 화면·소리 | 모니터 선택·재선택, 출력 장치 선택, 기본 장치 추적/고정, 시스템 소리 켜기/끄기, 소리 없이 녹화 허용 | 선택 장치, 연결·지원 상태, 변경 시 버퍼 초기화 안내 |
| 설정 → 기록·저장 | 보관 시간, 기본 저장 시간, 기록 부족 시 동작, 저장 폴더 찾아보기·열기 | 예상 메모리, 실제 가용 기록, 저장 시간 오차 안내 |
| 설정 → 화질·자원 | 해상도 상한, fps, 비트레이트, 압축·총 메모리 상한, 저장 대기 수 | 설정 유효성, 예산 초과 이유, 적용된 프로필 |
| 설정 → 단축키 | 최근 기록 저장 키 변경·테스트·기본값 복원 | 현재 적용 키, 편집 중 키, 충돌·등록 상태 |
| 설정 → 일반 | Windows 로그인 시 자동 실행, 알림 켜기/끄기, 설정 기본값 복원 | 자동 실행 적용 상태 |
| 저장 작업·기록 | 작업 상태 조회, 대기·진행 작업 취소, 완료 영상 열기, 폴더에서 보기, 실패 사유 보기 | 접수·대기·저장 중·완료·실패·취소, 요청/실제 길이, 파일 경로 |
| 상태·진단 | 장치 재연결 시도, 오류 상세 보기, 진단 정보 복사·파일 내보내기 | 메모리, encoder 이름, drop 수, epoch, 큐, 최근 오류 |

고급 설정은 GUI의 접힌 영역에서 제공한다. 샘플레이트·키프레임 간격처럼 초기 버전에서 고정한 내부 값은 읽기 전용으로 표시하고, 변경 기능을 도입할 때 GUI도 함께 제공한다. 후속 마이크 기능은 입력 장치·음량·음소거를, 정밀 저장 기능은 저장 모드 선택을 GUI에 추가해야 완료로 인정한다.

메인 화면의 가장 눈에 띄는 동작은 **[최근 60초 저장]** 버튼이다. 옆에 이번 저장 길이와 **[기본 저장 시간으로 되돌리기]**를 배치한다. 이번 저장 길이는 GUI 버튼에만 적용하며 기본 설정을 변경하지 않는다. 전역 단축키와 트레이의 저장 항목은 항상 설정의 기본 저장 길이를 사용하고 각각 해당 길이를 표시한다.

첫 실행 흐름은 앱 실행 → 모니터·소리 확인 → 보관 시간·저장 폴더·단축키 설정 → [녹화 시작]이다. 사용자는 이후 모든 설정을 GUI에서 수정할 수 있다.

### 9.2 저장 및 상태별 동작

- 버튼·단축키·트레이는 같은 `SaveCoordinator`를 호출한다. GUI를 열거나 파일 대화상자를 기다린 뒤 저장 시점을 정하지 않는다.
- 저장 버튼 클릭 또는 단축키 이벤트 접수 시 T를 고정한다. 전역 단축키 저장은 메인 창을 앞으로 가져오지 않는다.
- 저장은 비동기로 처리하고 즉시 접수 상태를 표시한다. UI 스레드에서 완료까지 기다리지 않는다.
- 폴더는 설정에서 미리 선택한다. 단축키 입력 때마다 저장 위치를 묻지 않는다.
- 기록 부족 정책은 **[있는 만큼 저장]**을 기본으로 하고 **[설정 길이가 모두 쌓인 경우만 저장]**을 선택 가능하게 한다. 후자는 키프레임에 따른 약간 긴 시작 구간을 없애는 기능이 아니다.
- 목표보다 짧은 기록은 실제 저장 가능 길이를 버튼 주변에 표시하고, 저장 결과에도 부분 기록임을 명시한다.
- 유효한 데이터가 없거나 복구 중이면 저장 버튼을 비활성화하고 이유를 표시한다. 같은 상태의 단축키 입력도 저장을 생성하지 않고 상태 알림을 제공한다.
- 저장 중에도 예산과 큐 여유가 있으면 추가 저장을 허용한다. 초과 시 버튼 옆과 작업 화면에 이유를 표시한다.
- 일시정지는 수집을 멈추고 기존 버퍼를 비운다. 재개는 새 epoch에서 시작한다. 이미 접수한 저장 작업은 유지한다.
- 녹화 중지는 수집과 미저장 버퍼를 정리하고 앱은 열린 상태로 둔다. 기존 저장 작업은 계속 처리한다.
- 버퍼 비우기는 미저장 기록만 제거하고 녹화 중이면 즉시 새 기록을 쌓는다. 이미 접수한 저장 작업의 참조는 유지한다.
- 앱 종료는 새 요청을 막고 진행 상태를 표시하며 저장 작업에 최대 10초의 완료 기회를 준 뒤 미완료 작업을 취소·정리한다.
- 작업 취소는 해당 작업의 partial 파일과 참조를 정리한다. 완료 영상 삭제 기능은 초기 범위에 포함하지 않는다.
- 완료·실패 알림은 메인 창이 닫혀 있어도 확인 가능하게 하며, 알림을 꺼도 작업 화면에 결과를 남긴다.

### 9.3 사용자 지정 전역 단축키

1. 설정의 단축키 화면에서 **[키 변경]**을 누른다.
2. 키 조합 입력 필드에 원하는 조합을 누르면 읽기 쉬운 형태로 표시한다. 예: `Ctrl+Alt+R`.
3. 수정 키만 입력한 경우, 지원하지 않는 조합, OS 예약 키와 충돌하는 조합을 검증하고 이유를 설명한다.
4. **[적용]** 시 실제 전역 등록 성공을 확인한다. 성공한 경우에만 새 키를 설정 파일에 저장하고 기존 키를 해제한다.
5. 새 키 등록 실패 시 기존 단축키와 설정을 유지하고 다른 키를 선택하도록 안내한다. 동일한 키 재적용은 별도 변경 없이 처리한다.
6. **[단축키 테스트]** 모드에서는 해당 입력의 수신 여부만 표시하고 영상 저장은 실행하지 않는다. 테스트 종료 후 일반 저장 동작으로 복귀한다.
7. **[기본값 복원]**도 동일한 충돌 검증을 거친다. 앱 재시작 때 저장된 키를 재등록하며 충돌하면 GUI 저장 버튼을 유지하고 등록 실패를 알린다.

기본 제안은 `Ctrl+Shift+F9`다. 일반 앱에 포커스가 있거나 메인 창이 트레이에 숨겨져 있어도 등록된 단축키로 저장한다. 키를 누르고 있는 동안 자동 반복 저장하지 않으며, 단축키 편집·테스트 모드에서는 실제 저장 동작을 억제한다. 키 입력 전체를 감시하는 방식 대신 전역 키 조합 등록을 사용한다.

### 9.4 트레이·접근성·창 동작

- 창의 X 버튼은 트레이로 숨기며 최초 1회 안내한다. 명시적인 [앱 종료]는 메인 메뉴와 트레이에 모두 제공한다.
- 트레이 메뉴는 창 열기, 최근 기본 시간 저장, 시작/일시정지/재개/중지, 설정, 종료를 제공한다. 트레이는 메인 GUI의 보조 조작 경로다.
- 탭 이동, Enter/Space 버튼 실행, 접근성 이름, 고대비·DPI 확대를 지원하고 색상만으로 상태를 전달하지 않는다.
- 저장 처리 중에도 창 이동·설정 조회·작업 취소가 응답해야 한다.
- 오류는 한국어 설명과 해결 행동을 함께 제시하며 내부 오류 코드만 표시하지 않는다.

### 9.5 설정 영속화 예시

다음 JSON은 GUI 설정의 내부 저장 형식이다. 사용자가 직접 편집하지 않아도 모든 사용자 설정을 화면에서 변경할 수 있어야 한다.

위치: `%LOCALAPPDATA%\ReplayCapture\settings.json`

```json
{
  "schemaVersion": 1,
  "buffer": { "retentionSeconds": 60, "maxEncodedMiB": 256 },
  "save": { "defaultSeconds": 60, "mode": "fast", "partialHistoryPolicy": "save-available", "folder": "<사용자가 선택한 폴더>" },
  "video": { "monitorId": "<선택한 모니터 식별자>", "maxWidth": 1920, "maxHeight": 1080, "fps": 30, "bitrateMbps": 8, "keyframeSeconds": 1 },
  "audio": { "systemEnabled": true, "deviceMode": "follow-default", "allowVideoOnly": false, "microphoneEnabled": false, "sampleRate": 48000, "bitrateKbps": 192 },
  "limits": { "totalBudgetMiB": 512, "maxQueuedSaves": 3 },
  "hotkeys": { "saveRecent": "Ctrl+Shift+F9" },
  "notificationsEnabled": true,
  "startWithWindows": false
}
```

시간은 양의 정수 초로 검증하고 `defaultSeconds <= retentionSeconds`를 강제한다. 예산상 불가능한 조합은 적용 전에 거절하고 예상 필요량을 표시한다. 파일은 임시 파일 후 교체 방식으로 저장한다.

보관 시간을 늘리면 이후부터 긴 기록이 채워진다. 줄이면 오래된 미참조 GOP부터 제거한다. 해상도·fps·장치·코덱 변경은 새 epoch로 시작하며 이전 버퍼가 초기화된다는 안내를 표시한다. 이미 접수된 저장 작업은 기존 스냅샷 설정으로 마친다.

## 10. 메모리·성능 예산

압축 버퍼 예상 크기의 기본식은 다음과 같다.

```text
payload bytes ≈ (video bit/s + audio bit/s) × seconds ÷ 8
총 필요량 = payload + 키프레임 여유 + 인코더 지연 + pin된 구간
           + GPU 표면 + PCM 큐 + 인코더 내부 메모리 + 앱 오버헤드
```

8Mbps 영상과 192kbps 오디오 기준의 단순 payload 추정:

| 시간 | 압축 데이터 크기, decimal MB |
|---|---:|
| 60초 | 약 61.44MB |
| 120초 | 약 122.88MB |
| 300초 | 약 307.20MB |
| 600초 | 약 614.40MB |

설정 예시의 256MiB 압축 상한에서는 600초를 이 품질로 보관할 수 없다. 시간·비트레이트·메모리 상한을 함께 조정해야 한다. 1080p BGRA 원본을 30fps로 60초 보관하면 약 14.93GB가 필요하므로 원본 프레임 전체 보관은 하지 않는다.

초기 성능 목표는 1080p30, H.264 하드웨어 인코딩, 60초 버퍼, 로컬 SSD에서 정한다. CPU 평균 5% 이하를 탐색 목표로 삼되 CPU 전체 기준임을 명시하고 실제 장비별 결과를 공개한다. 총 앱 메모리 512MiB 이내, 정상 부하에서 프레임 누락 1% 미만, GUI·단축키 저장 요청 접수 p95 200ms 이내, 빠른 저장 완료 p95 5초 이내가 제안 목표다.

GPU 드라이버·코덱 내부 할당까지 완전히 통제할 수 있다고 가정하지 않는다. 앱 소유 버퍼는 하드 상한을 적용하고 전체 메모리는 관측·회복 정책으로 관리한다. 과부하 시 캡처 큐의 오래된 미인코딩 프레임을 버려 지연을 제한하고 사용자에게 품질 저하를 알린다. 소프트웨어 인코딩으로 전환할 경우 720p30 등 보수적인 프로필을 명시적으로 적용한다.

## 11. 예외와 복구 정책

| 상황 | 처리 | 사용자에게 보이는 결과 |
|---|---|---|
| 녹화 시작 후 20초에 60초 저장 | 20초 내 실제 가용분 저장 | 부분 기록 길이 표시 |
| 모니터 분리 | 캡처 중단, 재선택 또는 재연결 | 선택 대상 없음 |
| 화면 잠금·절전 | 캡처 일시 중지 및 미저장 버퍼 비우기 | 재개 후 새 기록 축적 |
| GPU device lost | 제한적 재초기화, 새 epoch | 복구 중 / 반복 실패 시 오류 |
| 출력 장치 변경 | follow-default이면 재연결 | 일시적 무음 경고 |
| 보관 목표보다 메모리 부족 | 오래된 GOP 제거, 길이 감소 보고 | buffer_limited |
| 저장 작업 폭주 | 새 작업 거절, 기존 작업 보호 | BUSY 및 재시도 안내 |
| 디스크 부족 | 실패 처리, partial 정리 | 저장 실패, 녹화 상태 유지 |
| 단축키 충돌 | 기존 유효 키와 GUI 저장 버튼 유지, 실패 표시 | GUI에서 다른 키 설정 안내 |
| RDP·전체화면 앱 | 별도 호환 테스트 | 검증되지 않은 환경 표시 |
| 보호 콘텐츠·보안 데스크톱 | OS 제한 준수, 우회 없음 | 일부 내용 캡처 불가 안내 |

자동 복구는 초기 3회까지 지수적 지연으로 시도하고 실패하면 오류 상태를 유지한다. 화면 소스 손실과 단순 무변화를 구분하는 상태·이벤트 기반 로직을 둔다.

## 12. 사용자 제어와 데이터 관리

- 녹화 여부가 보이는 트레이 아이콘과 즉시 일시정지·종료 동작을 제공한다.
- 최초 실행 시 화면·시스템 소리·마이크 범위를 안내한다. 마이크와 자동 실행은 명시적 설정으로만 켠다.
- 수집 데이터는 기본적으로 로컬에서 처리한다. 외부 전송 기능은 초기 범위에 없다.
- 로그에는 화면 이미지, PCM 데이터, 키 입력 내용, 창 제목을 기록하지 않는다. 오류·성능·장치 상태 중심으로 남긴다.
- 메모리 버퍼를 사용해도 OS 페이지 파일이나 크래시 덤프에 데이터가 기록되지 않는다고 보장하지 않는다.
- 사용자 제어는 프로세스 내부 GUI·단축키 경로로 한정한다. 재실행 시 기존 창을 표시하는 메시지는 화면 표시 이외의 데이터·제어 요청을 수락하지 않는다.
- 전역 단축키는 특정 키 조합 등록으로 처리하고 일반 키 입력 전체를 수집하지 않는다.

## 13. 예정 소스 구조와 책임

```text
ReplayCapture/
  CMakeLists.txt
  CMakePresets.json
  src/
    app/             # 수명주기, 단일 인스턴스, 트레이
    ui/              # 대시보드, 설정, 작업 기록, 진단, 접근성
    hotkeys/         # 사용자 키 입력, 전역 등록, 충돌·반복 처리
    actions/         # GUI·단축키 공통 동작, 비동기 상태 전달
    capture/         # WGC, D3D11, 모니터 변경
    audio/           # WASAPI, 리샘플링, 선택 마이크 혼합
    timing/          # QPC 정규화, drift, 프레임 스케줄러
    encode/          # MFT 열거·설정, H.264/AAC 출력
    buffer/          # GOP 인덱스, 샘플 참조, 예산·삭제
    export/          # 구간 선택, 작업 큐, MP4, finalize
    config/          # 스키마, 검증, 설정 마이그레이션
    diagnostics/     # 메트릭, 오류 코드, 제한된 로그
  tests/
    unit/
    integration/
    fixtures/        # 시간코드 화면·플래시/비프 생성기
    endurance/
  packaging/         # 설치·제거, 자동 실행 설정
  docs/
    architecture.md
    gui-and-hotkeys.md
    validation-report.md
    troubleshooting.md
```

주요 인터페이스는 `CaptureSource`, `AudioSource`, `VideoEncoder`, `AudioEncoder`, `ReplayBuffer`, `SaveCoordinator`, `Mp4Exporter`, `ActionDispatcher`, `HotkeyManager`로 분리한다. 시간·프레임·장치 이벤트를 주입할 수 있어야 실제 장치 없이 버퍼 경계와 동시성 테스트가 가능하다.

## 14. 단계별 실행 계획

1인 개발 기준 약 7~9주를 초기 추정으로 잡는다. Windows 미디어 API 경험, 실제 GPU 장비 확보, 드라이버 문제에 따라 달라진다. UI부터 완성하지 말고 위험도가 높은 캡처·오디오·압축·MP4 연결을 먼저 검증한다.

| 단계 | 예상 기간 | 구현·산출물 | 통과 기준 / 실패 시 분기 |
|---|---|---|---|
| P0 요구사항 고정 | 1~2일 | OS·장비 행렬, 저장 길이 계약, 화면별 기능 표, 설정 스키마, 프로젝트 빌드 | 모든 사용자 기능의 GUI 진입점 확정 |
| P1 미디어 수직 실험 | 4~6일 | WGC+WASAPI+H.264/AAC→10초 MP4, 인코더 목록 | 화면·시스템 소리 재생, 초기 sync 확인. MF 압축 연결 실패 시 muxer 대체 실험 |
| P2 버퍼·저장 코어 | 5~7일 | GOP 버퍼, pin, T 기준 컷, partial/finalize | 최근 60초 반복 저장 중 캡처 연속. 메모리 무한 증가 시 다음 단계 중지 |
| P3 GUI·단축키 | 6~9일 | 대시보드, 전체 설정, 작업 관리, 진단, 트레이, 키 편집·테스트 | GUI만으로 전 기능 수행, 백그라운드 단축키 저장·변경·충돌·재시작 유지 확인 |
| P4 복구·자원 제어 | 4~6일 | 장치 전환, 잠금·복귀, 메모리·디스크 실패 처리 | 실패를 정상 성공으로 보고하지 않고 복구·정리 |
| P5 품질 검증 | 4~6일 | 재생·길이·sync·장시간·GPU 행렬 보고서 | 필수 시험 및 성능 게이트 충족 |
| P6 패키지·문서 | 2~3일 | 설치 산출물, 사용자 가이드, 제한사항, 버전 고정 | 깨끗한 PC 설치·실행·제거 및 일반 권한 확인 |

P1에서는 빠른 저장에 필요한 IDR 제어, B-frame 억제, AAC 지연, 압축 H.264/AAC의 MP4 직접 기록을 반드시 확인한다. 하나라도 해결되지 않으면 UI 확장 전에 설계를 수정한다. 단계 종료마다 실행 환경·원시 측정치·실패 사례를 `validation-report.md`에 남긴다.

마이크 혼합, 정밀 길이 저장, 디스크 버퍼, 4K60은 핵심 버전 출시 이후 각각 별도 일정과 완료 기준을 둔다.

## 15. 검증 계획과 출시 게이트

### 15.1 단위·통합 시험

| 시험 | 입력·상황 | 합격 기준 |
|---|---|---|
| 시간 컷 | 키프레임 사이 S, 다양한 T | S 직전 IDR 선택, T 이후 프레임 제외 |
| 길이 설정 | 5·60·120·600초, 잘못된 값 | 예산 내 설정 수락, 불가능한 값 명시적 거절 |
| 워밍업 | 가용 20초, 요청 60초 | 있는 만큼 저장은 부분 성공, 전체 길이 필수는 실행 불가 이유 표시 |
| 버퍼 삭제 | 시간·바이트 한계, pin 존재 | 디코딩 가능한 GOP 유지, 참조 손상 없음 |
| 동시 저장 | 연속 10회 요청 | 제한 내 처리, 초과 BUSY, 메모리 상한 유지 |
| 정지 화면 | 2분간 화면 변화 없음 | 저장 길이가 줄어들거나 타임라인 멈춤 없음 |
| 오디오 무음 | 재생→무음→재생 | 구간 길이 유지, 무음 후 sync 유지 |
| 파일 오류 | 공간·권한·finalize 실패 주입 | 실패 코드, 완성 MP4로 오인할 파일 없음 |
| epoch 변경 | 모니터·코덱 변경 | 서로 다른 코덱 구간 혼합 없음 |
| GUI 기능 완결성 | 화면별 기능 표의 모든 항목 | 터미널·설정 파일 편집 없이 전 기능 완료 |
| 저장 입력 경로 | GUI 버튼·트레이·전역 단축키 | 같은 저장 처리와 시간 기준, 경로별 길이 규칙 준수 |
| 단축키 변경 | 새 키·충돌 키·기본값·재시작 | 성공한 키만 적용, 실패 시 기존 키 유지, 재시작 후 재등록 |
| 단축키 반복·편집 | 길게 누르기·키 편집·테스트 | 누름 1회당 저장 1회, 편집·테스트에서 저장 없음 |
| GUI 응답·접근성 | 저장 중 조작, DPI 확대, 키보드 이동 | UI 멈춤 없음, 버튼·상태·작업 취소 접근 가능 |
| 창 수명주기 | X·트레이 복귀·재실행·녹화 중지·종료 | 창 숨김과 녹화 중지·앱 종료를 명세대로 구분 |

### 15.2 실제 장치 검증

- 시간코드가 표시되는 화면과 동시에 발생하는 플래시·비프를 테스트 소스로 사용한다.
- 60초 파일의 시작·중간·끝에서 시청각 차이의 절댓값 100ms 이하를 초기 합격 기준으로 제안한다.
- 2시간 상주 후에도 마지막 저장의 sync 기준을 만족하고, 초기 대비 추가 drift가 50ms를 넘지 않는지 측정한다.
- 키프레임 간격 1초 이하 프로필에서 시작점 초과 보관이 1초+1프레임 이내인지 확인한다.
- 정상 부하에서 실제 끝 시점이 T보다 100ms 넘게 뒤처지지 않는지 확인한다. 장애 시에는 누락을 정확히 보고해야 한다.
- Windows 기본 재생기와 별도의 디코더로 처음부터 끝까지 재생·디코딩한다. 검증용 도구 버전도 기록한다.
- Intel·NVIDIA·AMD 장비는 확보된 실제 장비별로 결과를 표시한다. 미시험 장비를 지원 검증 완료로 묶지 않는다.
- 노트북 내장/외장 GPU 전환, 오디오 장치 분리, 기본 출력 변경, 잠금·절전 복귀, 다중 모니터 선택·분리를 시험한다.
- 8시간 상주 시험에서 마지막 안정 6시간의 메모리 증가 추세, handle·GPU 자원 누수, 프레임 누락, 저장 지연을 기록한다.

### 15.3 필수 출시 조건

1. GUI 버튼과 사용자 지정 전역 단축키로 기본 60초 화면·시스템 소리가 저장되고 GUI에서 시간을 변경할 수 있다.
2. 저장 중 캡처가 계속되고 반복 저장 파일이 모두 재생된다.
3. 시간·동기화·메모리 관련 필수 기준을 만족한다.
4. 소리 수집 실패·부분 기록·저장 실패를 사용자가 구분할 수 있다.
5. 잠금·장치 변경 이후 제한된 복구 또는 분명한 오류 안내가 동작한다.
6. 일반 사용자 권한으로 깨끗한 검증 PC에서 설치·실행·제거가 가능하다.
7. GUI만으로 모든 기능을 조작할 수 있고 단축키 변경·충돌 처리·재시작 유지가 검증된다.
8. 검증 보고서에 실행하지 않은 항목과 알려진 제한을 남긴다.

## 16. 배포와 운영

- 우선 x64 ZIP 개발 패키지, 안정화 후 설치 패키지로 배포한다.
- 설정·로그는 LocalAppData, 완성 영상은 사용자가 선택한 폴더에 저장한다.
- 자동 실행은 사용자 단위 설정으로 제공하고 제거 시 해당 등록을 정리한다.
- 제거 시 저장된 영상을 자동 삭제하지 않는다. 설정·로그 삭제 여부는 별도로 선택한다.
- 공개 배포 전 의존 라이선스·런타임 포함 여부·서명 방식을 확정한다. 서명으로 모든 OS 경고가 사라진다고 보장하지 않는다.
- 최초 버전은 자동 업데이트 기능 없이 버전별 패키지와 릴리스 노트를 제공한다.

## 17. 착수 순서와 최종 산출물

첫 작업은 **선택한 모니터 화면과 시스템 소리를 10초 MP4로 기록하는 최소 수직 실험**이다. 그 다음 동일 압축 출력을 60초 링 버퍼로 바꾸고 최소 GUI의 시작·저장 버튼과 전역 저장 단축키를 연결한다. 이 경로가 검증된 뒤 설정 UI·복구·패키징을 확장한다.

최종 산출물은 `ReplayCapture.exe`, 설치 또는 ZIP 패키지, 한국어 사용자 가이드, GUI·단축키 명세, 아키텍처 문서, 실제 장비 검증 보고서다. 본 요청의 산출물은 이 계획서이며 프로그램 구현과 성능 시험은 아직 수행하지 않았다.
